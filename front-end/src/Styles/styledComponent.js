import { Box, TextField } from '@mui/material';
import { styled } from '@mui/system';

export const MyBox = styled(Box)({
  width: '90%',
  paddingLeft: '40%',
  paddingTop: '12%',
});

export const MyTextFiel = styled(TextField)({
  width: '15%',
});
